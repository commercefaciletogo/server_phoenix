defmodule Commercefacile.Web.Api.Mobile.AuthenticationController do
    use Commercefacile.Web, :controller

    def register(conn, _params) do
        
    end

    def login(conn, _params) do
        
    end

    def code(conn, _params) do
        
    end

    def phone(conn, _params) do
        
    end

    def reset(conn, _params) do
        
    end
end