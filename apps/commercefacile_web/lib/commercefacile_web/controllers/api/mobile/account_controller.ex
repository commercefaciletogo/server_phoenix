defmodule Commercefacile.Web.Api.Mobile.AccountController do
    use Commercefacile.Web, :controller

    def ads(conn, _params) do
        
    end

    def information(conn, _params) do
        
    end

    def password(conn, _params) do
        
    end

    def shop(conn, _params) do
        
    end
end