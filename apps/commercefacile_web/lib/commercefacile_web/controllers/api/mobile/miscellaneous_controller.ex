defmodule Commercefacile.Web.Api.Mobile.MiscellaneousController do
    use Commercefacile.Web, :controller

    def categories(conn, _params) do
        
    end

    def locations(conn, _params) do
        
    end
end