defmodule Commercefacile.Web.Api.Mobile.UploadController do
    use Commercefacile.Web, :controller

    def upload(conn, _params) do
        
    end

    def remove(conn, _params) do
        
    end
end